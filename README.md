# Core Four Orientation Threshold

Minimal, presence-first orientation artifact for Core Four. This repo contains a single React component that renders the orientation copy with quiet styling and no persistence, analytics, or interaction logic.
